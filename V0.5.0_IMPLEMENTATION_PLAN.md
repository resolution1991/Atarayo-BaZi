# Atarayo-BaZi v0.5.0 详细构建实施方案

状态：已实施、完成验收并发布（2026-07-31）
编制日期：2026-07-30
当前基线：`v0.4.0`（本地提交与标签已完成，尚未推送）
目标版本：`v0.5.0`
版本定位：建立可配置、可追溯、可回归的排盘计算基础设施，并把月柱与起运计算提升到离线分钟级节气精度。

## 1. 本版本完成后的产品结果

用户完成升级后，应当得到以下完整闭环：

1. 可以在“排盘设置”中选择排盘口径，设置在断网、退出和重启后继续保留。
2. 新建命盘时，计算引擎显式接收当前设置，不从页面或全局变量中隐式读取口径。
3. 每条新历史记录都保存应用版本、计算引擎版本、节气数据版本和完整设置快照。
4. `v0.4.0` 及更早记录继续展示原结果，标记为“旧版默认口径”，升级时不自动重算。
5. 用户可从旧记录手动执行“按当前设置重新排盘”；系统创建一条新记录，旧记录原样保留。
6. 月柱以分钟级“节”边界计算，起运使用精确到分钟的前后节气时刻。
7. 流年大运页能说明起运方向、前后节气、时间差、换算公式、起运时间和所用口径。
8. 旧基准盘在旧版默认口径下保持不变；所有关键时间边界都有可复现的自动化用例。

## 2. 范围边界

### 2.1 纳入 v0.5.0

- 排盘设置页和本地设置持久化。
- `CalculationSettings`、`CalculationProfile`、`CalculationSnapshot` 数据模型。
- 23 点/0 点换日口径。
- 早晚子时口径。
- 年柱边界：立春或农历正月初一。
- 生肖边界：立春或农历年。
- 四柱显示方向。
- 默认身强身弱流派。
- 神煞规则集总开关和规则集版本。
- 干支关系规则总开关和规则集版本。
- 历史记录 schema 升级和无损迁移。
- 分钟级二十四节气离线数据、生成脚本、来源说明和哈希。
- 分钟级月柱计算。
- 分钟级起运计算与过程展示。
- 手动按当前设置重新排盘。
- 自动化回归、H5 构建、APK 构建和 Android 模拟器升级验证。

### 2.2 明确不纳入

- 真太阳时、经纬度、城市库、海外时区和夏令时。
- 云同步、账号、远程 API。
- 用户自定义神煞单条规则。
- 用户自定义干支关系单条规则。
- 新增命理解释、白话论断或 AI 分析。
- 对全部旧记录自动批量重算。
- 正式签名、远端推送、Git 标签和 GitHub Release；这些等待用户另行明确。

### 2.3 固定时间假设

`v0.5.0` 暂时把用户输入理解为中国标准时间 `UTC+08:00`，不追溯 1900 年前后的地方历史时差。节气数据内部保存 UTC 时刻，计算前按固定 `UTC+08:00` 转换。地点、时区与真太阳时统一留到 `v0.6.0`。

## 3. 当前代码基线与必须解决的差距

| 领域 | 当前 `v0.4.0` 状态 | `v0.5.0` 必须完成的改造 |
| --- | --- | --- |
| 核心入口 | `calculateBazi(input, lunarData)`，无设置参数 | 增加显式 profile 计算入口，保留旧口径兼容入口 |
| 换日 | `calendar.ts` 固定 `hour >= 23` 查次日 | 根据设置确定有效排盘日 |
| 年月柱 | 直接读取日级农历表中的 `ganzhi_year/month` | 年柱按所选边界计算；月柱按分钟级“节”计算 |
| 子时 | 23 点固定使用次日日干推时柱 | 把早晚子时行为固化为经过确认和测试的规则矩阵 |
| 起运 | `luck.ts` 查月柱切换日，并按当天 `00:00` 估算 | 使用前后“节”的真实分钟时刻并输出完整换算过程 |
| 设置 | 无设置数据层和设置页 | 新增设置 service、页面、默认值、校验和恢复默认 |
| 身强流派 | 命盘页临时变量，默认传统派 | 新建命盘按设置快照默认展示，单盘仍可临时切换 |
| 神煞/关系 | 始终计算、始终展示 | 按快照中的规则集开关决定计算和展示 |
| 历史记录 | `{ id, created_at, data }` | schema v2 信封、计算输入、来源关系、设置与版本快照 |
| 迁移 | 无 schema 和迁移器 | 读取时识别 v1，惰性补元数据，不重算 `data` |

## 4. 关键产品与技术决策

### 4.1 旧版默认口径

定义唯一常量 `LEGACY_V0_4_PROFILE`，它是旧记录解释和回归测试的唯一依据：

```ts
{
  id: "legacy-v0.4",
  label: "旧版默认口径",
  settings: {
    dayBoundary: "zi-begin",
    ziHourMode: "unified",
    yearBoundary: "lunar-new-year",
    zodiacBoundary: "lunar-new-year",
    pillarDisplayOrder: "year-to-hour",
    defaultStrengthSchool: "traditional",
    shenSha: { enabled: true, ruleSet: "legacy-v1" },
    relations: { enabled: true, ruleSet: "legacy-v1" },
    timeZoneOffsetMinutes: 480
  }
}
```

这份 profile 的目的不是声称旧算法已经采用分钟级规则，而是冻结升级前的可见行为。旧记录继续直接使用已保存的 `data`，不得用该 profile 在迁移时重算。

新建命盘使用 `standard-v0.5` profile。它的用户设置默认值与 `legacy-v0.4` 一致，但计算引擎改用分钟级节气，因此出生在节气切换日边界附近时，月柱和起运结果允许发生有依据、可解释的变化。两者不能使用同一个 profile ID。

### 4.2 设置不用一组松散布尔值

为避免“23 点换日关闭但早晚子时拆分开启”等难以解释的组合，核心模型使用枚举：

```ts
export type DayBoundary = "midnight" | "zi-begin";
export type ZiHourMode = "unified" | "split";
export type YearBoundary = "lichun" | "lunar-new-year";
export type ZodiacBoundary = "lichun" | "lunar-new-year";
export type PillarDisplayOrder = "year-to-hour" | "hour-to-year";
export type StrengthSchool = "traditional" | "academic";

export interface RuleSetSelection {
  enabled: boolean;
  ruleSet: "legacy-v1";
}

export interface CalculationSettings {
  schemaVersion: 1;
  dayBoundary: DayBoundary;
  ziHourMode: ZiHourMode;
  yearBoundary: YearBoundary;
  zodiacBoundary: ZodiacBoundary;
  pillarDisplayOrder: PillarDisplayOrder;
  defaultStrengthSchool: StrengthSchool;
  shenSha: RuleSetSelection;
  relations: RuleSetSelection;
  timeZoneOffsetMinutes: 480;
}
```

页面可以使用“23 点换日”等易懂文案，但核心层不使用含义不清的布尔值。

### 4.3 子时规则是开发前的唯一业务确认门

当前代码只确定了“23:00 后日柱取次日”。“早晚子时拆分”在不同流派中还涉及晚子时的日柱和时干分别取哪一天，不能由开发自行猜测。

在进入阶段 2 前，由用户确认下表中 `split` 行的目标行为。建议采用“日柱只受换日边界控制；拆分子时只决定时干使用哪个民用日的日干”：

| 设置 | 22:59 | 23:00—23:59 | 00:00—00:59 |
| --- | --- | --- | --- |
| `zi-begin + unified` | 当日日柱，亥时 | 次日日柱；时干按次日日干 | 当日日柱；时干按当日日干 |
| `midnight + unified` | 当日日柱，亥时 | 当日日柱；时干按当日日干 | 新日柱；时干按新日日干 |
| `zi-begin + split` | 当日日柱，亥时 | 次日日柱、晚子时；时干按换日前民用日的日干 | 当日日柱、早子时；时干按当日日干 |
| `midnight + split` | 当日日柱，亥时 | 当日日柱、晚子时；时干按当日日干 | 新日柱、早子时；时干按新日日干 |

这一定义让“换日”决定日柱，让“拆分子时”只决定晚子时的时干依据，两项设置职责不重叠。若用户要求晚子时同时改写日柱，则需要另建明确的第三种 `dayBoundary`，不复用 `split` 暗示。

### 4.4 新旧计算入口并存

为保护现有 12 个 Python 基准结果，不直接破坏原函数：

```ts
calculateBazi(input, lunarData)
// 兼容入口：结果必须继续等于 v0.4.0 基准

calculateBaziWithProfile(input, context, profile)
// v0.5.0 页面使用的新入口，返回 data + trace
```

`context` 包含农历查表与节气查表；profile 必须由调用方显式传入。核心函数禁止直接读取 `uni.getStorageSync()`。

### 4.5 历史重排创建新记录

“按当前设置重新排盘”默认创建新记录，不覆盖旧记录：

- 新记录生成新 `id`。
- 新记录写入 `derivedFromRecordId`。
- 旧记录写入 `supersededByRecordId` 仅作为导航关系；旧 `data` 不改。
- 新旧记录可分别进入、导出和查看流运。
- 历史页用“旧版口径 / 当前口径”标签区分。

这样可以同时满足“不自动重算”和“不同设置结果互不污染”。

### 4.6 四柱显示方向只影响展示

核心数据始终使用 `year/month/day/hour` 命名，不反转数组、不改变导出字段语义。显示层根据快照决定列顺序，防止显示设置污染算法和关系定位。

## 5. 分钟级节气数据方案

### 5.1 推荐生成链路

推荐使用“仓库内 Python 生成脚本 + Skyfield + JPL DE440 星历”生成静态数据：

1. 使用 Skyfield 的 East Asia solar terms 算法求太阳视黄经每 15° 的过点。
2. 使用 JPL DE440 星历；JPL 标明其覆盖 1550—2650，可覆盖本项目 1900—2100。
3. 生成时记录 Skyfield 版本、星历文件名、星历 SHA-256、脚本版本、生成时间和输出 SHA-256。
4. 运行时只加载生成后的 TypeScript/JSON 数据，不携带 Python、Skyfield 或星历文件进入 APK。
5. 用紫金山天文台可查询年份抽取样本做人工交叉验证，但不依赖其在线服务作为运行时数据源。

参考：

- Skyfield Almanac solar terms：<https://rhodesmill.org/skyfield/almanac.html>
- JPL DE440/DE441 说明：<https://ssd.jpl.nasa.gov/doc/de440_de441.html>
- 紫金山天文台公农历与节气查询：<https://nddc.pmo.ac.cn/calculation/lunar>

### 5.2 数据结构

每年 24 条，约 4,824 条，体量远小于现有逐日农历表：

```ts
export interface SolarTermRecord {
  name: SolarTermName;
  longitude: number;
  utc: string;             // ISO 8601，保留秒
  cst: string;             // 固定 UTC+08:00 的展示值
  epochMinute: number;     // 运行时分钟比较
  kind: "jie" | "qi";
}
```

文件建议：

```text
src/data/solar-terms.generated.ts
src/data/solar-terms.ts
src/data/solar-terms.metadata.json
tools/generate_solar_terms.py
tools/verify_solar_terms.py
SOLAR_TERMS_DATA.md
```

### 5.3 覆盖范围

生成 `1899-12-01` 至 `2100-03-31`，而不是只生成界面允许的首末日。前后缓冲用于：

- 1900 年初查“前一个节”。
- 2100-02-08 查“后一个节”。
- 对边界日期稳定执行二分查找。

应用仍只允许当前农历数据范围 `1900-01-01` 至 `2100-02-08`。

### 5.4 数据验收

- 24 节气名称、顺序、经度映射完整。
- 任意相邻记录时间严格递增。
- 每个公历年具有 24 条记录，缓冲边界年份按实际区间校验。
- UTC 与固定 UTC+8 转换可逆。
- 数据输出 SHA-256 固定并写入元数据。
- 抽查 1900、1950、2000、2026、2050、2100；每年至少验证立春、惊蛰、冬至。
- 与权威/独立样本差异超过 1 分钟时停止合入并查明时标、取整或视黄经定义差异。

## 6. 计算引擎设计

### 6.1 计算上下文与结果

```ts
export interface CalculationContext {
  lunarCalendar: LunarCalendarSource;
  solarTerms: SolarTermLookup;
  appVersion: string;
  engineVersion: "bazi-engine-0.5.0";
  solarTermDataVersion: string;
}

export interface CalculationTrace {
  normalizedInput: BaziInput;
  effectiveDateTime: string;
  yearBoundary: { mode: YearBoundary; boundaryAt: string; result: string };
  monthBoundary: { previousJie: SolarTermPoint; nextJie: SolarTermPoint; result: string };
  dayBoundary: { mode: DayBoundary; rolled: boolean; result: string };
  hourRule: { ziHourMode: ZiHourMode; result: string };
}

export interface CalculatedChart {
  data: BaziData;
  trace: CalculationTrace;
}
```

### 6.2 四柱计算顺序

1. 解析并校验输入。
2. 把输入解释为固定 `UTC+08:00` 的本地民用时间。
3. 根据 `dayBoundary` 计算有效排盘日期。
4. 日柱从现有逐日表取有效日期的 `ganzhi_day`，保持存量日柱来源不变。
5. 年柱：
   - `lunar-new-year`：取原始出生民用日期对应农历表的 `ganzhi_year`。
   - `lichun`：比较出生分钟与当年立春；立春前按上一干支年，立春后按当年。
6. 月柱：
   - 以十二“节”而非十二“气”划月。
   - 二分查找出生时刻之前最近的“节”。
   - 月支由寅至丑确定，月干按年干五虎遁确定。
7. 时柱：
   - 时支仍按双小时。
   - 时干只从明确的有效日干和已确认的子时规则推导。
8. 丰富十神、藏干、身强和格局。
9. 根据规则集开关计算神煞、关系；关闭时返回明确的 disabled 状态，不伪装成“未命中”。
10. 返回结果与 trace，由历史 service 写入快照。

### 6.3 年柱与生肖分离

年柱和生肖各自读取设置，不能默认绑定：

- 年柱可按立春，生肖可按农历年。
- 年柱可按农历年，生肖可按立春。
- 首页农历年份选择器继续展示农历年生肖；命盘结果页展示的是本盘按设置计算的生肖。

需要新增纯函数：

```ts
resolveYearGanZhi(...)
resolveZodiac(...)
resolveMonthGanZhi(...)
resolveEffectiveDay(...)
resolveHourGanZhi(...)
```

### 6.4 起运计算

沿用现有顺逆规则与“三天折一年”的换算口径，只替换节气时刻精度：

- 阳年男、阴年女顺行；阴年男、阳年女逆行。
- 顺行：出生时刻 → 后一个“节”。
- 逆行：前一个“节” → 出生时刻。
- 时间差保留原始分钟数。
- 继续沿用现有换算：
  - 4,320 分钟 = 1 年。
  - 360 分钟 = 1 月。
  - 12 分钟 = 1 日。
  - 1 分钟 = 2 小时。
- `LuckStartInfo.precision` 从 `"day"` 扩展为 `"minute"`。
- 输出 `differenceMinutes`、可读时间差、每一步商和余数、最终起运时间。

不在本版本改变顺逆行规则或换算比例；如后续需要其他流派，新增 `luckStartRuleSet`，不静默替换。

## 7. 历史记录 schema 与迁移

### 7.1 schema v2

```ts
export interface HistoryRecordV2 {
  schemaVersion: 2;
  id: string;
  createdAt: string;
  normalizedInput: BaziInput;
  data: BaziData;
  calculation: CalculationSnapshot;
  provenance: {
    migratedFrom?: "v1";
    derivedFromRecordId?: string;
    supersededByRecordId?: string;
  };
}

export interface CalculationSnapshot {
  appVersion: string;
  engineVersion: string;
  solarTermDataVersion: string;
  profileId: string;
  profileLabel: string;
  settings: CalculationSettings;
}
```

### 7.2 迁移策略

采用“读取时规范化、一次性回写、绝不重算”的迁移：

1. `readHistory()` 读取原 key `bazi_history_records`。
2. 无 `schemaVersion` 的记录识别为 v1。
3. 从已保存的 `birth_info` 还原规范化公历输入。
4. 原 `data` 对象原样保留。
5. 添加 `legacy-v0.4` 快照，`engineVersion` 和节气数据版本标记为 `legacy-day-table`。
6. 先将原始数组备份到 `bazi_history_records_backup_v1`。
7. 校验迁移后记录数、ID、姓名、四柱和创建时间。
8. 校验通过后回写 v2；失败则继续返回原数据的只读规范化结果，不覆盖存储。
9. 设置 `bazi_history_schema_version = 2`，避免每次重复迁移。

备份 key 不在 v0.5.0 自动删除，为回退保留恢复点。

### 7.3 设置存储

新增独立 key：

```text
bazi_calculation_settings_v1
```

读取流程：读存储 → schema 校验 → 字段白名单归一化 → 缺失字段补默认值 → 返回深拷贝。损坏值不导致启动失败，回退到推荐 profile，并在设置页显示一次“已恢复默认设置”提示。

## 8. 页面与交互

### 8.1 排盘设置页

新增 `src/pages/settings/settings.vue`，从首页底部导航进入。

页面分组：

1. 日期边界
   - 换日：23 点 / 0 点。
   - 子时：统一子时 / 早晚子时。
2. 年份边界
   - 年柱：立春 / 农历正月初一。
   - 生肖：立春 / 农历年。
3. 显示与默认流派
   - 四柱顺序：年月日时 / 时日月年。
   - 默认身强流派：传统派 / 学术派。
4. 规则集
   - 神煞规则：启用 / 关闭，显示规则集版本。
   - 干支关系：启用 / 关闭，显示规则集版本。
5. 当前计算环境
   - App 版本。
   - 引擎版本。
   - 节气数据版本和哈希短值。
   - 时间口径 `UTC+08:00`。
6. 操作
   - 保存设置。
   - 恢复推荐设置。

设置采用“编辑草稿 + 点击保存”，返回手势不自动写入，避免误触立即改变后续排盘。

### 8.2 首页

- 预览使用已保存的当前设置，但不创建历史记录。
- 预览区域增加一行口径摘要，例如“23点换日 · 立春年界 · 传统派”。
- 底部导航增加“设置”。
- 点击“开始排盘”时一次性读取设置并深拷贝，计算和保存共用同一快照，避免计算过程中设置变化。

### 8.3 命盘详情

- 显示“排盘口径”入口，弹层展示该记录自己的快照，而不是当前全局设置。
- 默认身强流派读取记录快照；用户在当前页面临时切换不修改历史快照。
- 规则关闭时对应区域显示“此命盘创建时未启用神煞/干支关系”，不显示空列表。
- 四柱列顺序读取记录快照。
- 旧记录显示“旧版默认口径”标签和“按当前设置重新排盘”按钮。
- 新记录也允许“按当前设置重新排盘”；仅当当前设置与快照不同才突出按钮。

### 8.4 历史页

- 每条记录显示口径标签。
- 新旧重排记录不自动合并、不互相覆盖。
- 重新排盘成功后打开新记录，并 Toast 提示“已创建新命盘，原记录已保留”。

### 8.5 流年大运页

替换“当前按本地日级节令数据估算”提示，新增可展开的“起运计算过程”：

- 顺排/逆排及判定依据。
- 出生时间。
- 前一个节、后一个节。
- 实际参与计算的起点和终点。
- 相差天、时、分和总分钟。
- 三天折一年换算过程。
- 起运时间。
- 引擎、节气数据与设置口径。

## 9. 文件级实施清单

### 9.1 新增文件

```text
src/core/calculation-profile.ts
src/core/solar-terms.ts
src/core/calculation-trace.ts
src/data/solar-terms.generated.ts
src/data/solar-terms.ts
src/data/solar-terms.metadata.json
src/services/settings.ts
src/services/history-migration.ts
src/pages/settings/settings.vue
tools/generate_solar_terms.py
tools/verify_solar_terms.py
tests/calculation-profile.test.ts
tests/calendar-boundaries.test.ts
tests/solar-terms.test.ts
tests/history-migration.test.ts
tests/settings.test.ts
tests/luck-minute.test.ts
SOLAR_TERMS_DATA.md
```

### 9.2 重点修改文件

| 文件 | 改造内容 |
| --- | --- |
| `src/core/types.ts` | 设置、profile、snapshot、trace 类型 |
| `src/core/calendar.ts` | 有效日期、年柱、月柱、子时纯函数 |
| `src/core/calculate.ts` | 新 profile 入口和 legacy 兼容入口 |
| `src/core/luck.ts` | 分钟级节气、计算 trace、precision |
| `src/core/shen-sha.ts` | 规则集入口，不读取 UI 状态 |
| `src/core/relations.ts` | 规则集入口，不读取 UI 状态 |
| `src/services/history.ts` | schema v2、迁移、重排关系 |
| `src/pages/index/index.vue` | 当前设置计算、口径摘要、设置导航 |
| `src/pages/chart/chart.vue` | 快照展示、规则开关、重排、显示顺序 |
| `src/pages/history/history.vue` | 口径标签与新旧记录关系 |
| `src/pages/fortune/fortune.vue` | 分钟级起运过程展示 |
| `src/pages.json` | 注册设置页 |
| `package.json` | 新增生成、校验、测试命令，最终升到 0.5.0 |
| `src/manifest.json` | 收口阶段升到 `0.5.0 (5)` |
| `tools/build_android_webview_apk.sh` | APK 文件名和版本同步 |

## 10. 分阶段构建顺序

任何阶段未满足退出条件，不进入下一阶段。

### 阶段 0：口径确认与基线冻结

任务：

- 确认早晚子时规则矩阵。
- 确认推荐默认设置；建议 `standard-v0.5` 的用户设置默认值与 `legacy-v0.4` 一致，但使用分钟级新引擎，并在边界结果变化时显示计算依据。
- 运行并保存当前全部测试结果。
- 增加 `v0.4.0` 代表性记录迁移夹具，不使用用户真实姓名。
- 记录现有 APK 中历史记录数量，用于升级验证。

退出条件：

- 子时规则形成书面确认。
- `v0.4.0` 全部测试和 H5 构建通过。
- 基准 fixture 已进入仓库。

你负责：

- 确认子时拆分的业务口径。
- 确认新装用户的设置默认值是否继续与旧版一致。

Codex 负责：

- 冻结代码和数据基准。
- 把确认结果转成类型、规则说明和测试表。

### 阶段 1：设置模型与存储

任务：

- 实现 settings/profile 类型、默认常量、校验器、深拷贝和 settings service。
- 先不修改计算结果。
- 完成设置持久化、损坏数据回退、恢复默认测试。

退出条件：

- 设置重启恢复测试通过。
- 任意非法枚举不会进入核心计算。
- `LEGACY_V0_4_PROFILE` 不可被运行时修改。

### 阶段 2：历史 schema v2 与迁移

任务：

- 实现 v1/v2 类型守卫、迁移器、备份和回滚保护。
- 新记录开始保存 normalized input 和 calculation snapshot。
- 旧记录继续使用原 `data`。

退出条件：

- 0、1、多条、部分损坏记录迁移用例通过。
- 迁移前后记录数、ID、四柱、姓名和时间一致。
- 重复执行迁移幂等。

### 阶段 3：分钟级节气数据

任务：

- 固定生成环境和依赖版本。
- 编写生成与验证脚本。
- 生成带缓冲区的数据和元数据。
- 与外部样本交叉验证。

退出条件：

- 数据连续性、顺序、时区、哈希测试通过。
- 抽查年份与独立来源差异不超过 1 分钟，或差异已有书面解释并经用户接受。
- H5 和 APK 运行时不依赖网络、Python 或星历文件。

### 阶段 4：profile 驱动的四柱引擎

任务：

- 实现年月日时四柱纯函数。
- 增加 `calculateBaziWithProfile()`。
- 保留 `calculateBazi()` legacy 行为。
- 接入年柱、生肖、月柱、换日、子时设置。

退出条件：

- 原 12 个 core baseline 完全不变。
- 22:59、23:00、23:59、00:00、00:59 边界矩阵通过。
- 立春和农历新年前后各一分钟用例通过。
- 十二个“节”前后各一分钟月柱用例通过。
- 同一输入不同 profile 结果彼此独立。

### 阶段 5：分钟级起运

任务：

- `luck.ts` 改用 solar term lookup。
- 补齐差值、换算和起运 trace。
- 保持顺逆规则与换算比例不变。

退出条件：

- 顺行、逆行、恰好落在节上、跨年查节用例通过。
- 起运结果可从 trace 手算复现。
- 旧记录仍可按原已保存四柱打开；只有手动重排产生新结果。

### 阶段 6：设置页与各页面接入

任务：

- 完成设置页、首页摘要和导航。
- 命盘页使用记录快照。
- 历史页显示口径标签并支持重排。
- 流运页展示计算过程。
- 应用显示/隐藏神煞和关系规则集。

退出条件：

- 当前设置只影响之后新建/手动重排的命盘。
- 改设置不会改变已打开或已保存记录。
- 四柱反向显示不改变导出字段和算法结果。
- 关闭规则集时 UI 文案明确，页面无异常。

### 阶段 7：全量回归与离线验证

自动化：

```bash
pnpm run test:core
pnpm run test:relations
pnpm run test:luck
pnpm run test:shen-sha
pnpm run test:strength
pnpm run test:export
pnpm run test:fortune-export
pnpm run test:rule-docs
pnpm run test:full-data
pnpm run test:settings
pnpm run test:migration
pnpm run test:solar-terms
pnpm run test:boundaries
pnpm run test:luck-minute
pnpm run build:h5
```

手工/模拟器：

- 从 `0.4.0` APK 覆盖升级到 `0.5.0`。
- 断网冷启动。
- 旧历史数量与四柱不变。
- 设置保存、杀进程、重启后恢复。
- 用两套设置排同一出生时间，结果和标签独立。
- 旧记录手动重排后形成新记录，旧记录仍可进入。
- 分钟级起运过程可展开、可读。
- 清空 crash buffer 后走完整流程，最终无崩溃。

退出条件：

- 自动化全绿。
- Android 升级、持久化、断网和 crash 检查通过。
- 不声明新增 Android 网络或定位权限。

### 阶段 8：版本收口

任务：

- 同步 `package.json`、`manifest.json`、Android `versionCode=5`、APK 文件名。
- 更新 README、开发日志、打包检查表、规则说明和数据来源文档。
- 构建 `bazi-mvp-0.5.0-debug.apk`。
- 校验 v1/v2/v3 签名、权限、APK 内数据文件和 SHA-256。

退出条件：

- 文档、源代码、APK 版本完全一致。
- APK 安装与最终冷启动通过。
- 形成待用户验收的变更清单和已知限制。

只有用户明确要求后，才执行 Git 提交、`v0.5.0` 标签、推送和 GitHub Release。

## 11. 测试用例最小矩阵

### 11.1 换日与子时

每种合法设置至少覆盖：

- 同一日期 `22:59`。
- `23:00`。
- `23:59`。
- 次日 `00:00`。
- `00:59`。
- 月末、年末各一组。
- 节气发生在 23 点附近时至少一组组合边界。

断言四柱、有效日期、子时标签、trace 和原始输入均正确。

### 11.2 年柱与生肖

- 立春前一分钟、恰好立春、立春后一分钟。
- 农历正月初一前一分钟、当日 00:00、后一分钟。
- 立春与春节先后顺序不同的年份至少各一例。
- 年柱边界和生肖边界交叉组合 4 组。

### 11.3 月柱

- 十二个“节”各自前一分钟、恰好发生、后一分钟。
- 跨公历年小寒。
- 立春同时影响年柱和月柱。
- 只在“气”发生时不换月柱。

### 11.4 起运

- 阳年男、阴年女顺行。
- 阴年男、阳年女逆行。
- 出生恰好落在节上。
- 出生距目标节不足 12 分钟。
- 跨月、跨年寻找前后节。
- 原有 luck timeline 的大运干支顺序不回归。

### 11.5 历史与设置

- 空存储首次启动。
- v1 单条/多条迁移。
- v2 重复读取。
- 部分字段损坏。
- 设置存储损坏。
- 修改全局设置后打开旧记录。
- 同一命盘两次重排使用不同 snapshot。
- 重排失败时不产生半条记录。

## 12. 风险、预警和控制措施

| 风险 | 影响 | 控制措施 |
| --- | --- | --- |
| “早晚子时”语义未确认 | 四柱结果产生争议 | 阶段 0 书面确认后才编码 |
| 当前逐日表年月柱来源不明 | 新旧盘在边界日差异 | legacy 入口冻结；新引擎 trace 显示边界依据 |
| 节气时标/时区处理错误 | 月柱和起运整体偏移 | 内部 UTC、固定 UTC+8 展示、独立样本交叉验证 |
| 直接修改 `BaziData` 导致旧基准失败 | 大面积回归 | 新结果信封和兼容入口并存 |
| 迁移覆盖用户历史 | 数据不可恢复 | 原 key 备份、先校验后回写、迁移幂等 |
| 设置是全局变量 | 历史记录被污染 | 每条记录深拷贝完整 snapshot |
| 关闭规则后被误解为“未命中” | 产品语义错误 | 引擎和 UI 使用 disabled 状态 |
| 一次性同时改算法与 UI | 难以定位问题 | 严格按数据层→引擎→页面顺序推进 |
| 节气依赖进入运行时 | 破坏离线原则 | 生成期依赖与运行期数据彻底分离 |

## 13. 工作量与协作节奏

以当前代码规模估算，完成到可交付 debug APK 约需 7—10 个有效开发日，不含等待口径确认和真机测试：

| 阶段 | 预计 |
| --- | ---: |
| 口径确认、基线冻结 | 0.5 天 |
| 设置模型与历史迁移 | 1.5—2 天 |
| 节气生成、验证和数据接入 | 1—1.5 天 |
| 四柱 profile 引擎 | 1.5—2 天 |
| 分钟级起运 | 0.5—1 天 |
| 页面与交互 | 1.5—2 天 |
| 回归、APK、文档收口 | 1—1.5 天 |

建议采用“一阶段一验收”的协作方式：

- Codex 每阶段提交：实现结果、自动化证据、差异说明、下一阶段风险。
- 用户在阶段 0 确认口径，在阶段 6 审核页面文案与交互，在阶段 7/8 完成主观体验和真机验收。
- 未通过的阶段只在本阶段修正，不把已知问题滚入下一阶段。

## 14. Definition of Done

`v0.5.0` 只有同时满足以下条件才算构建完成：

- [x] 子时、换日、年柱、生肖、显示、流派和规则集口径均可配置。
- [x] 新计算全部由显式 profile 驱动。
- [x] 旧记录不重算且有“旧版默认口径”标记。
- [x] 手动重排生成新记录并保留来源记录。
- [x] 历史记录保存应用、引擎、节气数据和设置快照。
- [x] 分钟级节气数据覆盖要求范围，生成链路、来源和哈希在仓库内可追溯。
- [x] 月柱边界和起运计算精确到分钟且可解释。
- [x] 旧 core baseline 完全通过。
- [x] 新边界、迁移、设置、节气和起运测试全部通过。
- [x] H5 生产构建通过。
- [x] Android 覆盖升级、断网、重启持久化、冷启动和 crash 检查通过。
- [x] `0.5.0 (5)` 元数据、文档和 APK 一致。
- [x] 交付前功能与模拟器验收完成；用户侧复核可在交付后进行。

## 15. 下一步

本方案的阶段 0—8 已全部完成。2026-07-31 已创建发布提交 `4f64b0e` 和注释标签 `v0.5.0`，推送远端并创建附带 APK 的 GitHub Release。

1. Android 真机复测与正式签名包。
2. 启动 `v0.6.0` 真太阳时与离线出生地建设。
