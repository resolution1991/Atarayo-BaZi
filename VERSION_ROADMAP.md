# Atarayo-BaZi 内部版本演进计划

状态：内部工作文档，仅供当前产品/项目决策与 Codex 后续执行使用。
基线：`v0.5.0` 已完成代码、数据、文档、调试 APK 与 Android 模拟器本地交付收口；Git 版本节点与正式发布等待用户另行指令。
总原则：安装完成后，在无网络、无账号、无远程 API、无云端数据的情况下，所有核心功能必须完整可用。

## 固定约束

1. 核心计算必须是端侧确定性逻辑，不调用远程服务。
2. 所需历法、节气、城市、规则、古籍等数据随包提供，或由用户导入离线数据包。
3. 新算法不得静默替换旧算法：
   - 旧口径继续作为默认值，或通过明确迁移说明变更。
   - 存量命盘保存创建时的计算版本和排盘口径。
   - 有流派分歧时提供可见开关。
4. 每项命理规则至少包含：
   - 规则说明。
   - 纯函数实现。
   - 正例、反例、边界用例。
   - 与存量基准盘的回归测试。
5. 不进入核心路线：
   - 云端 AI。
   - 在线地图或在线地理编码。
   - 云同步、云备份、WebDAV 自动备份。
   - 在线社区、咨询、课程和内容流。
   - 依赖服务器的会员鉴权。
6. 本计划只规定功能演进顺序。Git 提交、打标签、APK 发布和 GitHub Release 均在用户明确要求后执行。

## v0.4.0：记录与信息导出收口

定位：把 `v0.3.0` 后已经完成的交互改动整理为一个可发布的小版本，不引入新的排盘口径。

### 已实现

- 历史记录页：
  - 干支按五行着色。
  - 搜索提示改为“请输入命主姓名”。
  - 性别单选筛选。
  - 出生年份单选筛选。
  - 属相单选筛选。
  - “显示全部”清除筛选条件。
- 命盘详情：
  - 增加“导出信息”。
  - 导出公历/农历、性别、两派身强身弱、格局、四柱、主辅星、藏干、神煞和干支关系。
  - 不导出姓名。
- 流年大运：
  - 增加未来十年流年大运导出。
  - 从当前年开始连续导出十年。
  - 每年正确匹配跨界后的大运。
  - 导出原局四柱及大运、流年的主星、辅星和藏干。

### 收口结果（2026-07-30）

- [x] `package.json`、`src/manifest.json`、Android `versionCode` 和 APK 文件名同步至 `0.4.0 (4)`。
- [x] README、开发日志和打包检查表已更新。
- [x] 全部规则与导出测试通过。
- [x] H5 生产构建通过。
- [x] `bazi-mvp-0.4.0-debug.apk` 重新生成，v1/v2/v3 签名通过，无 Android 权限声明。
- [x] Android API 37 模拟器覆盖安装、最终冷启动和 crash buffer 检查通过。
- [x] 升级后原有 5 条历史记录保留。
- [x] 历史姓名搜索、性别/属相筛选、清除筛选、候选年份加载及干支五行颜色验证通过。
- [x] 命盘信息复制通过，Toast 和导出字段正确，未包含姓名。
- [x] `2026—2035` 十年流运复制通过，Toast、年份数量、字段及按钮居中样式正确。
- [ ] Android 真机复测。
- [ ] 正式签名包。
- [x] Git 提交与 `v0.4.0` 标签。
- [ ] 推送远端与 GitHub Release；等待用户明确指令。

### 不纳入

- 不修改四柱算法。
- 不修改大运起运算法。
- 不新增真太阳时。
- 不新增数据库迁移。

## v0.5.0：排盘口径与分钟级节气

定位：先建立可配置、可追溯的计算口径，再扩展更多专业字段。

实施状态：本地可交付版本已完成。实施依据与完成定义见 `V0.5.0_IMPLEMENTATION_PLAN.md`。

### 功能

- 增加“排盘设置”页面。
- 建立 `CalculationProfile` / `CalculationSettings` 数据模型。
- 支持：
  - 23 点换日口径。
  - 早晚子时是否拆分。
  - 年柱以立春或农历正月初一为界。
  - 生肖以立春或农历年为界。
  - 四柱显示方向。
  - 默认身强身弱流派。
  - 神煞规则集开关。
  - 干支关系规则开关。
- 每条历史命盘保存：
  - 创建时的应用版本。
  - 计算引擎版本。
  - 排盘设置快照。
- 使用离线分钟级二十四节气数据替代当前日级估算。
- 起运结果显示：
  - 起运时间。
  - 前后节气。
  - 时间差。
  - 换算过程。
  - 使用的计算口径。
- 现有命盘迁移：
  - 标记为“旧版默认口径”。
  - 不自动重算。
  - 提供手动“按当前设置重新排盘”。

### 数据与技术

- 节气数据至少覆盖当前农历数据范围 `1900-01-01` 至 `2100-02-08`。
- 节气表必须随包内置。
- 节气生成脚本、数据来源说明和哈希纳入仓库。
- 增加跨节气分钟边界测试。
- 增加 22:59、23:00、23:59、00:00 等换日边界测试。

### 验收门槛

- 旧基准盘在“旧版默认口径”下结果不变。
- 分钟级节气前后各一分钟的月柱结果可复现。
- 同一命盘在不同设置下能产生独立结果，历史记录不互相污染。
- 断网启动、排盘、切换设置、重启恢复全部正常。

### 收口结果（2026-07-31）

- [x] `CalculationProfile`、设置页、排盘 trace、历史快照与关联式重排完成。
- [x] 4,808 条分钟级节气数据覆盖 `1899-12-07 15:04` 至 `2100-03-20 21:06`，生成脚本、来源、版本和哈希可追溯。
- [x] 原 `calculateBazi()` 兼容入口及 12 个旧基准盘结果保持不变。
- [x] 设置、迁移、节气、换日/立春/十二节边界和分钟级起运测试完成；共 14 组自动化测试全部通过。
- [x] H5 桌面及 `390 × 844` 移动视口交互验收通过，无横向溢出和功能性控制台错误。
- [x] Android API 37 从旧版本覆盖升级成功，原有 5 条记录无重算迁移并保留备份。
- [x] 设置持久化、四柱倒序、分钟级起运过程及“按当前设置重排”端内验收通过。
- [x] 关闭 Wi-Fi 与移动数据后离线冷启动通过，最终 crash buffer 为空。
- [x] 最终 APK 已关闭 WebView 调试接口，无 Android 权限声明，v1/v2/v3 签名通过。
- [x] `0.5.0 (5)` 源码、文档与 `bazi-mvp-0.5.0-debug.apk` 一致。
- [ ] Android 真机复测与正式签名包；不属于本次本地 debug 交付范围。
- [ ] Git 提交、`v0.5.0` 标签、远端推送与 GitHub Release；等待用户明确指令。

## v0.6.0：真太阳时与离线出生地

定位：在 `v0.5.0` 的计算设置模型上增加出生地和时间校正。

### 功能

- 排盘表单增加：
  - 国家/地区。
  - 省/州。
  - 城市。
  - 经度。
  - 纬度。
  - 标准时区。
- 内置离线城市数据：
  - 首版优先中国大陆主要城市。
  - 港澳台和海外城市后续以离线数据包扩展。
- 支持手动输入经纬度。
- 支持三种时间口径：
  - 输入的当地标准时间。
  - 地方平太阳时。
  - 真太阳时。
- 显示校正过程：
  - 经度时差。
  - 均时差。
  - 最终真太阳时。
  - 是否跨时辰、跨日、跨节气。
- 历史命盘保存出生地点和校正结果快照。
- 增加常用地点收藏。

### 数据与技术

- 不接在线地图。
- 不做详细地址搜索。
- 不做在线时区查询。
- 城市库必须携带数据版本。
- 历史时区和夏令时：
  - 中国大陆先按明确的历史时区规则实现。
  - 海外时区必须等待离线 IANA 时区数据接入后再开放。

### 验收门槛

- 北京、乌鲁木齐、拉萨等经度差异明显的城市有校正用例。
- 校正前后跨时辰、跨日的样例可复现。
- 不选择地点时继续沿用标准时间排盘。
- 旧命盘不因升级自动改变。

## v0.7.0：专业细盘基础字段

定位：补齐专业排盘常用的静态命盘信息，暂不增加白话论断。

### 功能

- 纳音。
- 十二长生。
- 胎元。
- 胎息。
- 命宫。
- 身宫。
- 空亡。
- 小运。
- 月令司令。
- 出生前后节气信息。
- 日主通根：
  - 本气根。
  - 中气根。
  - 余气根。
- 透干信息。
- 得令、得地、得势的结构化展示。
- 专业细盘页面：
  - 默认保持当前简洁命盘页。
  - 通过“基础盘 / 专业细盘”切换进入。
- 每个新增字段支持点击查看：
  - 定义。
  - 计算规则。
  - 本盘计算过程。

### 规则边界

- 命宫、身宫、胎元、胎息存在流派差异时必须配置规则集。
- 十二长生明确阴干顺逆口径。
- 空亡明确按日柱旬空及其他扩展口径。
- 暂不增加无法确定前置条件的合化成功结论。

### 验收门槛

- 每个字段至少有 10 个跨边界用例。
- 专业细盘字段可独立导出。
- 字段缺失或规则关闭时使用明确占位，不抛出页面异常。

## v0.8.0：岁运深化

定位：从“查看大运流年列表”升级为“分析原局与岁运如何发生作用”。

### 功能

- 流日。
- 流时。
- 任意日期快速跳转。
- “今天”快捷入口。
- 重点日期收藏。
- 建立统一的动态盘层级：
  - 原局。
  - 大运。
  - 流年。
  - 流月。
  - 流日。
  - 流时。
- 动态作用关系：
  - 大运 ↔ 原局。
  - 流年 ↔ 原局。
  - 大运 ↔ 流年。
  - 流月/流日/流时 ↔ 原局。
  - 流月/流日/流时 ↔ 大运和流年。
- 关系展示区分：
  - 原局内部关系。
  - 外来岁运关系。
  - 合、冲、刑、害、破、克。
  - 两两关系和三合/三会关系。
- 五行力量变化：
  - 原局基线。
  - 加入大运。
  - 加入流年。
  - 加入流月/日/时。
  - 逐层对比。
- 导出：
  - 所选日期细盘。
  - 指定年份全年流月。
  - 指定月份每日列表。

### 技术前置

- 统一 `TemporalPillar`、`LuckContext` 和动态关系接口。
- 不在页面中直接拼装规则结果。
- 流日、流时生成函数必须是纯函数。

### 验收门槛

- 跨大运年份自动切换正确。
- 立春前后流年口径与 `v0.5.0` 设置一致。
- 原局关系与岁运新增关系在 UI 和导出中可区分。
- 10 年、12 月和 31 日批量计算不出现明显卡顿。

## v0.9.0：本地案例工作台与数据安全

定位：把历史记录从简单列表升级为长期使用的离线案例库。

### 存储重构

- 建立本地数据库抽象。
- H5 优先使用 IndexedDB。
- Android 壳评估 SQLite 桥接；未接入前继续使用 IndexedDB。
- 数据实体：
  - Person。
  - Chart。
  - CalculationSettingsSnapshot。
  - Note。
  - Event。
  - Tag。
  - FavoriteDate。
- 提供从现有 `uni` storage 的一次性迁移。
- 数据库 schema 版本化。

### 功能

- 每个命盘：
  - 总体笔记。
  - 按大运笔记。
  - 按流年笔记。
  - 按日期记录事件。
  - 标签。
- 事件时间线：
  - 日期。
  - 类别。
  - 事件描述。
  - 当时大运、流年、流月快照。
  - 事前判断与事后复盘。
- 高级筛选：
  - 性别、年份、属相。
  - 四柱任意干支。
  - 日主。
  - 五行数量。
  - 身强身弱及流派。
  - 格局。
  - 十神组合。
  - 神煞组合。
  - 干支关系。
  - 标签。
- 对比模式：
  - 两个命盘并排。
  - 同一命盘两个年份并排。
- 本地数据安全：
  - 全量 JSON 备份。
  - 备份清单、时间、应用版本、schema 版本。
  - SHA-256 完整性校验。
  - 从本地文件恢复。
  - 单个命盘导入、导出。
  - 恢复前预览，不直接覆盖。

### 明确不做

- 不做云同步。
- 不做登录。
- 不做 WebDAV。
- 不自动上传任何命盘或笔记。

### 验收门槛

- 旧数据迁移不丢记录。
- 备份→清空测试环境→恢复后数据一致。
- 损坏备份能够拒绝导入。
- 1,000 条命盘下筛选和列表操作可接受。

## v0.10.0：喜用神、调候与证据链

定位：增加可解释的专业分析层，仍以规则结果为主，不生成泛化断语。

### 功能

- 扶抑喜用神。
- 调候用神。
- 格局用神。
- 忌神。
- 日主旺衰证据：
  - 得令。
  - 得地。
  - 得势。
  - 通根。
  - 透干。
  - 生扶与克泄耗。
- 不同流派切换：
  - 传统派。
  - 学术派。
  - 后续明确规则后增加其他流派。
- 五行力量计算过程：
  - 天干权重。
  - 地支权重。
  - 藏干拆分。
  - 月令修正。
  - 合化前后对比仅在合化条件完整后开放。
- 每个结论展示：
  - 结论。
  - 命盘证据。
  - 使用的规则。
  - 未满足的条件。
  - 存在的争议或其他流派观点。

### 规则边界

- 规则条件不完整时输出“暂不判定”，不做猜测。
- 不将多个流派结果合并成一个模糊结论。
- 不用神煞直接替代格局和五行判断。

### 验收门槛

- 每类用神至少建立正例、反例、临界例。
- 导出内容包含规则版本。
- 页面结论和导出结论来自同一纯函数结果。

## v0.11.0：离线解释与学习

定位：降低阅读门槛，但不引入云端 AI。

### 功能

- 结构化模板解释：
  - 命局总体结构。
  - 日主特点。
  - 十神分布。
  - 五行偏向。
  - 当前大运主题。
  - 当前流年关系。
- 每段解释必须能展开“为什么”：
  - 对应字段。
  - 对应规则。
  - 对应命盘证据。
- 术语字典：
  - 天干地支。
  - 五行生克。
  - 十神。
  - 神煞。
  - 格局。
  - 用神。
  - 大运流年。
- 古籍资料：
  - 原文。
  - 出处。
  - 本地现代注释。
- 当前命盘作为学习示例。
- 内置少量经过校验的案例。
- 学习模式：
  - 分步骤读盘。
  - 隐藏结论。
  - 查看答案和规则依据。
- 解释内容随 APK 发布，不运行时联网更新。

### 明确不做

- 不做在线问答。
- 不调用大模型。
- 不生成自由文本式“AI 精批”。
- 不自动承诺事业、婚姻、健康等现实结果。

### 验收门槛

- 模板只能引用已计算出的结构化事实。
- 每个解释模板都有触发条件测试。
- 关闭解释层后，排盘和专业细盘不受影响。

## v1.0.0：离线专业版稳定基线

定位：停止扩功能，集中完成正式分发所需的稳定化和数据安全。

### 功能冻结范围

- `v0.4.0` 至 `v0.11.0` 已完成的离线功能。
- 不在 `v1.0.0` 临时加入合盘、择日、起名、六爻、紫微等新模块。

### 稳定化

- 统一所有计算引擎版本号。
- 完整数据迁移链：
  - `v0.3.0` → 当前版本。
  - 任意中间版本 → `v1.0.0`。
- 全量基准命盘回归。
- 节气、真太阳时、岁运、用神的边界回归。
- H5、Android WebView 壳的端侧性能检查。
- 大数据量历史库检查。
- 断网冷启动和全流程验证。
- Android：
  - 正式签名。
  - release APK。
  - 升级安装验证。
  - 主流 Android 版本和屏幕适配。
- iOS：
  - 仅在 Android 稳定后评估。
  - 如果无法保持完整离线能力，不阻塞 Android `v1.0.0`。
- 完成用户可见的数据备份提醒和隐私说明。

### 发布验收

- 飞行模式下完成：
  - 新建命盘。
  - 切换排盘口径。
  - 真太阳时排盘。
  - 查看专业细盘。
  - 查看未来岁运。
  - 写笔记。
  - 筛选案例。
  - 导出。
  - 备份和恢复。
- 无远程请求。
- 无账号依赖。
- 新安装和覆盖升级均通过。
- 本地备份可恢复到另一台设备。

## v1.0.0 后候选池

只有在核心专业工具稳定后再决定是否启动：

- 八字合盘。
- 多人合盘。
- 离线择日。
- 离线老黄历。
- 起名与姓名五行。
- 袁天罡称骨。
- 本地风水罗盘。
- 六爻。
- 紫微斗数。
- 奇门遁甲。
- 梅花易数。
- 本地通知和桌面小组件。
- 可选离线语言模型包。

候选池项目不得反向阻塞 `v1.0.0`。

## 当前执行顺序

1. 完成 `v0.5.0` APK 与 Android 覆盖升级验收。
2. 用户完成 `v0.5.0` 真机复测。
3. 用户明确要求后，再建立 Git 节点、推送或发布。
4. `v0.5.0` 稳定后再进入 `v0.6.0`。
