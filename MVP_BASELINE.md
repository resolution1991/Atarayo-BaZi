# MVP 0.1.0 基线说明

基线日期：2026-06-25

## 版本定位

当前版本作为八字排盘 uni-app 重构版的首个 MVP 基线，用于后续 Android / iOS 封包验证。

MVP 的目标是先跑通核心离线排盘闭环，不扩展原项目未稳定支持的命理功能。

## 已纳入功能

- 首页排盘：
  - 姓名输入。
  - 性别选择。
  - 公历出生时间输入。
  - 农历出生时间输入，并反查换算为公历时间。
  - 农历年份显示干支和生肖，例如 `1996（丙子-鼠）年`。
  - 农历月份、日期使用中文格式，例如 `腊月 十七日`。
  - 首页命盘预览。
- 命盘详情：
  - 公历、农历生日展示。
  - 四柱、主星、天干、地支、藏干、副星展示。
  - 身强身弱、格局展示。
  - 五行分布展示。
  - 四柱明细展示。
  - 姓名可直接编辑，并保存到本机历史记录。
- 历史记录：
  - 本机历史记录列表。
  - 历史记录搜索。
  - 单条删除。
  - 清空全部。
  - 点击记录进入命盘详情。
- 离线能力：
  - 核心排盘逻辑为 TypeScript 纯函数。
  - 农历/干支数据已生成为本地 TypeScript 数据。
  - 不依赖 Flask、远程 API、uniCloud 或在线农历接口。

## 暂不纳入 MVP

- 真太阳时。
- 出生地、经纬度、时区修正。
- 农历闰月的显式“闰”字选择器优化。
- 合盘。
- 八字论财。
- 名人库。
- 学堂/设置等非核心页面。
- 格局性格库完整展示。
- 导出、分享、云同步、备份。

## 核心逻辑边界

当前 MVP 沿用原 Python 项目的核心排盘规则。除低级、可验证错误外，不主动调整：

- 四柱计算口径。
- 23 点换日口径。
- 十神规则。
- 身强身弱规则。
- 格局判断规则。

## 验证命令

```bash
pnpm run test:core
pnpm run test:full-data
pnpm run build:h5
pnpm run build:app
pnpm run build:apk:debug
```

当前基线验证结果：

- `test:core` 通过，12 个核心基准样例一致。
- `test:full-data` 通过，12 个全量离线数据基准样例一致。
- `build:h5` 通过。
- `build:app` 通过，输出目录为 `dist/build/app`。
- `build:apk:debug` 通过，输出 `dist/apk/bazi-mvp-0.1.0-debug.apk`。

## 封包推进原则

1. 先完成 Android 调试包真机验证。
2. Android 安装、启动、排盘、历史记录保存均通过后，再处理正式签名包。
3. iOS 放在 Android 跑通后推进，单独处理 Apple Developer、证书、描述文件、真机/TestFlight 流程。
